import { ReactNode } from "react";

/**
 * Platform admin layout — super-admin area.
 * Sprint 1: stub only.
 */
export default function PlatformLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
