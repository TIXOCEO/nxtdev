import { ReactNode } from "react";

/**
 * Tenant route group layout.
 * Sprint 1: stub only — auth guards go here in Sprint 2.
 */
export default function TenantLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
