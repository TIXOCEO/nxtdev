import { ReactNode } from "react";

/**
 * Public layout — marketing pages, login, signup.
 * Sprint 1: stub only.
 */
export default function PublicLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
